import React from "react";
import { StyleSheet, Text, TouchableOpacity, View } from "react-native";

const CustomTabBar = () => {
    return (
        <View>
            <Text>cdscsdcsdcsd</Text>
        </View>
    );
};

const styles = StyleSheet.create({
    
});

export default CustomTabBar;